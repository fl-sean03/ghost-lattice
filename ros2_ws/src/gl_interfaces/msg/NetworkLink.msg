# Single pairwise link between two vehicles

string src                 # vehicle_id
string dst                 # vehicle_id
float64 quality            # 0.0 (dead) to 1.0 (perfect)
float64 latency_ms
bool active                # true if link is usable

export enum InteractionMode {
  SELECT = "select",
  PLACE_JAMMER = "place_jammer",
  PLACE_GPS_ZONE = "place_gps_zone",
  DRAW_SEARCH = "draw_search",
  KILL_DRONE = "kill_drone",
  SPAWN_EMITTER = "spawn_emitter",
}
